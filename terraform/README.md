# Terraform Foundation

This directory intentionally contains architecture-level Terraform patterns rather than a claim of a production enterprise landing zone.

Production implementations should integrate with the organization's approved modules, network topology, IAM model, policy-as-code, state backend, and CI/CD controls.
